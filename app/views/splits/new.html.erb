<div class="container page-3 m-3 px-3">
  <%= simple_form_for [@activity_cost, @split] do |f| %>
      <%= f.input :split_type, as: :check_boxes, collection: ['Evenly','Individual'], item_wrapper_class: 'inline'%>
      <%= link_to "Back"   %>
      <%= f.submit "Next" %>
    <% end %>
</div>








<%= link_to "Edit",  edit_group_activity_cost_path(@activity_cost.group, @activity_cost), class: "btn m-3"%>
