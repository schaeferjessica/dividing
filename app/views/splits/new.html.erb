<div class="container page-3 m-3 px-3">
  <%= simple_form_for [@activity_cost, @split] do |f| %>
      <%= f.input :split_type, as: :radio_buttons, collection: ['evenly', 'individualy']%>
      <%= link_to "Back",  edit_group_activity_cost_path(@activity_cost.group, @activity_cost), class: "btn btn-primary m-3"   %>
      <%= f.submit "Next", class: "btn btn-primary m-3" %>
    <% end %>
</div>
